# Cloudflare D1 (SQLite) — used ONLY by the Prisma CLI when generating migrations locally.
# In the Worker, D1 is accessed via the `DB` binding in wrangler.toml, not this URL.
# For local Prisma CLI use, point at a local sqlite file.
DATABASE_URL=file:./dev.db
